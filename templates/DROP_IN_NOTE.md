# Drop-in GitLab version

This package includes the **full** platform-agnostic AI4SDLC PromptOps Starter Pack (`.ai4sdlc/` + docs)
and also includes a **GitLab overlay already applied at the repo root** (`.gitlab/`, `AGENTS.md`, `CODEOWNERS`).

If you want the overlay as a separate reference copy, see `platform_overlays/gitlab/`.
